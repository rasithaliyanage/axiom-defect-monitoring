Generation mode: built-in image_gen.
Source: AI_NATIVE_PROBABILISTIC_ARCHITECTURE_MERMAID.md
Initial prompt: AI_NATIVE_INFOGRAPHIC_PROMPT.txt

Refinement 1:
Edit this architecture infographic, preserving its overall layout, all panels, icons, typography and most text. Correct these specific architecture arrows precisely: (1) In factory OT REMOVE both arrows from box 4 Uncertainty & aggregation directly to box 6 Persist before dispatch and to Sync & polling. Instead draw a clear arrow from box 4 into box 5 Approved QC policy. The decision flow MUST be 1 -> 2 -> 3 -> 4 -> 5 -> 6 -> Line control adapter, with an additional arrow ONLY from box 6 to Sync & polling. (2) The purple approved inspection package should have dashed configuration arrows into box 3 and box 5, not solid runtime arrows. (3) The dashed purple 'Approved learning releases to enterprise configuration' must terminate on Configuration & reporting, NOT RabbitMQ. (4) Add a thin blue evidence persistence arrow from Ingestion API routed neatly to DATA & EVIDENCE panel. (5) Change DMZ subtitle 'Secure, one-way-out connectivity' to 'Controlled OT / IT boundary', preserving 'Factory-initiated connections' elsewhere. Preserve all remaining architecture details and legible text. No new modules.

Refinement 2 (final):
Make ONE essential correction to this exact image, preserving everything else unchanged. The blue vertical arrow currently goes from purple box '4 Uncertainty & aggregation' down directly into blue box '6 Persist before dispatch'. DELETE that vertical arrow entirely. Replace it with a blue elbow connector starting at the BOTTOM of box 4, going down halfway into the white gap, then LEFT across the gap, then DOWN with its arrowhead entering the TOP RIGHT of amber box '5 Approved QC policy'. It must visibly connect 4 to 5, NOT 4 to 6. Leave the existing horizontal blue arrow from amber box 5 to blue box 6. Thus evidence must pass through QC before persistence. Ensure no arrow from 4 directly enters 6. Do not change any boxes, labels or other parts.
